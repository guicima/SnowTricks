{% extends 'base.html.twig' %}

{% block title %}
	{{ 'tricks.new'|trans }}
{% endblock %}

{% block body %}
	<div class="card m-5 p-5">
		<h1>{{ 'tricks.new'|trans }}</h1>

		{{ include('trick/_form.html.twig', {'button_label': 'actions.create'|trans}) }}

		<a class="ms-auto me-auto" href="{{ path('app_home_page') }}">{{ 'back.to.list'|trans }}</a>
	</div>
{% endblock %}
